import * as React from "react";
import Form from "rebul/lib/elements/form/Form";

const FormSample: React.SFC<{}> = (props: {}) => {
    return (
        <div className="example">
          <Form />
        </div>
    )
};

export default FormSample;
